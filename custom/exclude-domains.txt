# Исключения: эти домены НЕ попадут в итоговый proxy ruleset,
# даже если их захватывает широкий upstream domain_suffix.
#
# Обычная строка исключает домен и все поддомены:
# twitch.tv
#
# Только точный домен:
# full:api.example.com
